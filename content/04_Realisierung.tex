\chapter{Praktische Realisierung der Testumgebung und Verfahren}
\label{chap:real}

Der folgende Abschnitt beschreibt die Testumgebung für den Vergleich der Clustering-Algorithmen. Ziel ist eine realitätsnahe Umgebung, um Leistungsfähigkeit und Effizienz der Algorithmen unter kontrollierten Bedingungen zu bewerten. Die Testumgebung umfasst die eingesetzte Hardware und Software, die Konfiguration der Vektordatenbank sowie die Vorbereitung der Logdaten. Die Versuche finden in fünf Durchläufen statt, wobei eine zufällige Neuanordnung der Logdaten in den Proben erfolgt. Dies stellt sicher, dass die Tests die Algorithmen nicht nur punktuell, sondern auch im kontinuierlichen Betrieb unter realen Bedingungen prüfen.
Diese umfassende Testumgebung bildet die Grundlage für einen fundierten und nachvollziehbaren Vergleich der unterschiedlichen Clustering-Ansätze.

\section{Jupyter Notebooks: Interaktive Validierung der Clustering-Ansätze}
Eine geeignete Softwareumgebung ist essenziell für die Durchführung und Validierung der Tests. Prinzipiell lassen sich die erforderlichen Python-Skripte über die Konsole ausführen. Für explorative Analysen ist der Einsatz von Jupyter Notebooks jedoch effizienter und komfortabler.\footnote{Eine detaillierte Beschreibung der Architektur von Jupyter-Notebook ist hier zu finden: \url{https://docs.jupyter.org/en/latest/projects/architecture/content-architecture.html}}
Der entscheidende Vorteil gegenüber herkömmlichen Python-Dateien liegt in der interaktiven, zellenbasierten Struktur. Während ein klassisches Skript linear durchläuft und den Arbeitsspeicher anschließend freigibt, hält das Jupyter Notebook den Kernel-Zustand (Speicherinhalt) über die Ausführung einzelner Zellen hinaus aufrecht. Dies erlaubt die einmalige Ausführung rechenintensiver Vorgänge – wie das Laden großer Datensätze oder das Trainieren von Modellen. Anschließend lassen sich verschiedene Analyseschritte iterativ testen, ohne den gesamten Prozess neu zu starten.
Zudem unterstützt Jupyter den Ansatz des „Literate Programming“\footnote{Ein Programmierparadigma von Donald E. Knuth: \url{http://www.literateprogramming.com/index.html}}: Die Kombination von ausführbarem Code, formatiertem Text (Markdown) und visualisierten Ergebnissen in einem Dokument erhöht die Nachvollziehbarkeit. Grafiken und Diagramme erscheinen direkt im Kontext des Codes, was die Interpretation der Ergebnisse und die spätere Berichterstellung erleichtert.

\section{Docker: Isolierte und reproduzierbare Testumgebungen}

Die Testumgebung für das Clustering der Lognachrichten stellt zwei Hauptanforderungen: Reproduzierbarkeit und Isolation zur Vermeidung von Abhängigkeitsproblemen. Die Verarbeitung mit klassischen Algorithmen erfordert lediglich einige Python-Bibliotheken, die sich auch in einer virtuellen Umgebung installieren ließen. Für das Clustering mit der Vektordatenbank ist jedoch PostgreSQL mit der pg\_vector-Erweiterung notwendig. Statt einer schwergewichtigen virtuellen Maschine kommt daher Docker als leichtgewichtige Alternative zum Einsatz.
Docker ist eine Open-Source-Plattform für Containerisierung, die Anwendungen in isolierten Umgebungen ausführt. Im Gegensatz zur klassischen Virtualisierung mittels Virtual Machines (VMs) nutzt Docker die Betriebssystem-Virtualisierung.\footnote{Unter \url{https://docs.docker.com/get-started/docker-overview/} sind mehr Details zur Architektur von Docker zu finden} Die Container teilen sich den Kernel des Host-Betriebssystems, wodurch sie deutlich ressourceneffizienter arbeiten.
Den Kern einer Docker-Umgebung bilden das Docker-Image und der daraus resultierende Container. Ein Image fungiert als schreibgeschützte Vorlage mit Anwendungscode, Bibliotheken und Konfigurationsdateien. Ein instanziiertes Image wird zum Container, einer ausführbaren, isolierten Einheit. Die Kapselung aller Abhängigkeiten ermöglicht ein portables System, das auf verschiedenen Rechnern läuft. Da die Testumgebung zwei Container benötigt, bietet Docker Compose eine komfortable Lösung zur Orchestrierung. Dieses Werkzeug definiert und führt Multi-Container-Anwendungen aus. Die Konfiguration erfolgt deklarativ über eine YAML-Datei (`docker-compose.yml`), die Services, Netzwerke und Volumes spezifiziert. Ein einziger Befehl (`docker-compose up`) startet die gesamte Umgebung.


\section{Testsetup: Orchestrierung von PostgreSQL und Jupyter}
Abbildung \ref{lst:docker-compose} zeigt den Inhalt der `docker-compose.yml`-Datei der Testumgebung. Die Konfigurationsdatei definiert zwei zentrale Services: eine PostgreSQL-Datenbank mit pgvector-Erweiterung und einen Jupyter-Notebook-Container.
\newpage

\begin{lstlisting}[caption={Docker Compose Konfiguration für PGVector und Jupyter}, label={lst:docker-compose}]
services:
  pgvector:
    image: pgvector/pgvector:pg17
    container_name: pgvector_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vectordb
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vectordb"]
      interval: 10s
      timeout: 5s
      retries: 5

  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jupyter_notebook
    ports:
      - "8888:8888"
    volumes:
      - .:/notebooks
    environment:
      PGVECTOR_DB_URL: "postgresql+psycopg://postgres:postgres@pgvector:5432/vectordb"
    depends_on:
      pgvector:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgvector_data:
\end{lstlisting}

Der `pgvector`-Service basiert auf einem vorgefertigten Image und stellt eine PostgreSQL-Datenbank mit installierter pgvector-Erweiterung bereit. Er speichert Daten persistent in einem Docker-Volume und ist über Port 5432 erreichbar. Ein Healthcheck prüft regelmäßig die Einsatzbereitschaft der Datenbank.
Der `jupyter`-Service nutzt ein eigenes Image, das auf einem Dockerfile basiert. Abbildung 4 zeigt den Inhalt dieser Datei. Das Dockerfile verwendet ein Python-3.10-Image als Basis und setzt `/app` als Arbeitsverzeichnis. Es installiert essenzielle System-Abhängigkeiten: `build-essential` für Compiler, `libpq-dev` für PostgreSQL-Bibliotheken und `curl` für Downloads. Anschließend erfolgt die Installation des Python-Paketmanagers `uv`, der dem Systempfad hinzugefügt wird. Die Datei `pyproject.toml` definiert die Abhängigkeiten, die zusammen mit Jupyter, JupyterLab und ipywidgets im Image installiert werden. Das Arbeitsverzeichnis wechselt zu `/notebooks`, das über ein Volume mit dem Host-System geteilt ist. Der Container exponiert Port 8888 für den Zugriff auf die Weboberfläche. Der Startbefehl konfiguriert Jupyter Lab für den Zugriff über alle Netzwerkschnittstellen ohne Authentifizierung, was die lokale Entwicklung vereinfacht. Die Umgebungsvariable `PGVECTOR\_DB\_URL` konfiguriert die Datenbankverbindung. Die `depends\_on`-Direktive stellt sicher, dass der Jupyter-Container erst nach der Datenbank startet. So entsteht eine flexible und reproduzierbare Entwicklungs- und Testumgebung.


\section{Datenvorbereitung: Dimensionsreduzierung durch TF-IDF-Vektorisierung}
Um die textuellen Lognachrichten für nachfolgende Machine Learning Algorithmen nutzbar zu machen, ist eine Überführung in eine numerische Repräsentation erforderlich. Dies erfolgt im vorliegenden Ansatz mittels des TF-IDF-Verfahrens (Term Frequency-Inverse Document Frequency), welches die Relevanz einzelner Begriffe innerhalb der gesamten Log-Sammlung gewichtet. Das Vorgehen ist im Code Beispiel \ref{lst:text-tf-idf} zu sehen. 

Vor der Vektorisierung werden die Rohdaten aus der Spalte 'log' des Dataframes extrahiert und mittels der strip-Methode von führenden sowie abschließenden Leerzeichen bereinigt, um eine konsistente Datenbasis zu gewährleisten. Die eigentliche Transformation übernimmt die Klasse \texttt{TfidfVectorizer}.

\begin{lstlisting}[caption={Vektorisieren mit TF-IDF}, label={lst:text-tf-idf}]
# TF-IDF Parameter
TFIDF_MAX_FEATURES = 1000
TFIDF_MIN_DF = 2
TFIDF_MAX_DF = 0.95

# ===== TF-IDF VEKTORISIERUNG =====
# Logs aus dem df_labeled extrahieren
logs_clean = df_labeled['log'].str.strip().tolist()

vectorizer = TfidfVectorizer(
    max_features=TFIDF_MAX_FEATURES,
    min_df=TFIDF_MIN_DF,
    max_df=TFIDF_MAX_DF
)
X_tfidf = vectorizer.fit_transform(logs_clean)
\end{lstlisting}

Die Konfiguration des Vektorisierers zielt darauf ab, die Dimensionalität des entstehenden Feature-Raums zu kontrollieren und Rauschen zu minimieren. Der Parameter \texttt{TFIDF\_MAX\_FEATURES} beschränkt das Vokabular auf die 1000 signifikantesten Terme. Ergänzend filtern die Parameter \texttt{min\_df} und \texttt{max\_df} irrelevante Begriffe: Terme, die in weniger als zwei Log-Einträgen vorkommen (\texttt{min\_df = 2}), werden als Ausreißer ignoriert, während Begriffe, die in mehr als 95\,\% aller Logs erscheinen (\texttt{max\_df = 0.95}), als generische Stopwörter ausgeschlossen werden. Durch den Aufruf von \texttt{fit\_transform} wird das Vokabular gelernt und die Logdaten werden final in die gewichtete Matrix \texttt{X\_tfidf} überführt.

\section{k-Means in der Praxis: Implementierung und Zentroid-Zuweisung}
m Anschluss an die Vektorisierung erfolgt die semantische Gruppierung der Logdaten mittels des K-Means-Algorithmus. Dieses partitionierende Verfahren unterteilt den Datenraum in eine vordefinierte Anzahl von Clustern, wobei jeder Log-Eintrag dem Cluster mit dem nächstgelegenen Schwerpunkt (Zentroid) zugeordnet wird.

\begin{lstlisting}[caption={Clustern mit k-Means}, label={lst:k-means-cluster}]
# K-Means Parameter
KMEANS_N_CLUSTERS = 32
KMEANS_RANDOM_STATE = 42
# ===== K-MEANS CLUSTERING =====
kmeans = KMeans(
    n_clusters=KMEANS_N_CLUSTERS,
    random_state=KMEANS_RANDOM_STATE
)
df_labeled['kmeans_cluster_id'] = kmeans.fit_predict(X_tfidf)
\end{lstlisting}

Die Initialisierung des Algorithmus erfolgt durch die Klasse \texttt{KMeans}, wobei zwei wesentliche Parameter konfiguriert werden. Die Anzahl der zu bildenden Cluster (\texttt{n\_clusters}) wird für diesen Versuch auf 32 festgelegt. Um die Reproduzierbarkeit der Ergebnisse im Rahmen dieser Arbeit zu gewährleisten, wird der Zufallsgenerator über den Parameter \texttt{random\_state} auf den festen Wert $42$ fixiert. Dies stellt sicher, dass bei erneuter Ausführung des Codes trotz der stochastischen Initialisierung der Zentroide identische Zuordnungen entstehen.

Die Ausführung des Clusterings erfolgt durch die Methode \texttt{fit\_predict}, welche auf die zuvor erzeugte TF-IDF-Matrix \texttt{X\_tfidf} angewendet wird. In diesem Schritt berechnet der Algorithmus iterativ die Cluster-Zentren und weist jedem Log-Vektor ein Label zu. Die resultierenden Cluster-IDs werden abschließend als neue Spalte \texttt{'kmeans\_cluster\_id'} im Dataframe gespeichert, wodurch ein direkter Vergleich mit den Ergebnissen der anderen Algorithmen ermöglicht wird.

\section{DBSCAN in der Praxis: Dichtebasierte Gruppierung und Rauschfilterung}

Als alternativer Ansatz zum partitionierenden K-Means-Verfahren wird der
dichtebasierte Algorithmus DBSCAN (Density-Based Spatial Clustering of
Applications with Noise) eingesetzt. Im Gegensatz zu K-Means muss hierbei die
Anzahl der Cluster nicht a priori definiert werden; stattdessen bildet der
Algorithmus Gruppen basierend auf der lokalen Dichte der Datenpunkte im
Vektorraum. Dies ist für den vorliegenden Anwendungsfall von Vorteil, da die
tatsächliche Anzahl der Log-Templates im Datensatz im Vorfeld nicht bekannt ist.
Die praktische Umsetzung ist in Listing~\ref{lst:dbscan-cluster} dargestellt.

\begin{lstlisting}[caption={Clustern mit DBSCAN}, label={lst:dbscan-cluster}]
# DBSCAN Parameter
DBSCAN_EPS         = 0.6
DBSCAN_MIN_SAMPLES = 2
DBSCAN_METRIC      = 'cosine'

# ===== DBSCAN CLUSTERING =====
dbscan = DBSCAN(
    eps=DBSCAN_EPS,
    min_samples=DBSCAN_MIN_SAMPLES,
    metric=DBSCAN_METRIC
)
df_labeled['dbscan_cluster_id'] = dbscan.fit_predict(X_tfidf)
\end{lstlisting}

Die Konfiguration der DBSCAN-Instanz erfolgt über drei zentrale Parameter,
deren Wahl maßgeblich das Ergebnis beeinflusst.

\paragraph{Distanzmetrik: Cosinus-Distanz.}
Der Parameter \texttt{DBSCAN\_METRIC} legt die Distanzfunktion fest, anhand
derer die Nachbarschaft eines Datenpunktes bestimmt wird. In dieser Umsetzung
wird die Cosinus-Distanz gewählt, die sich aus der Cosinus-Ähnlichkeit ableitet:
\begin{equation}
    d_{\cos}(\mathbf{u}, \mathbf{v}) = 1 - \frac{\mathbf{u} \cdot \mathbf{v}}
    {\|\mathbf{u}\| \cdot \|\mathbf{v}\|}
\end{equation}
Die Wahl dieser Metrik ist für TF-IDF-Vektoren aus zwei Gründen besonders
geeignet. Erstens sind TF-IDF-Vektoren hochdimensional und dünn besetzt
(\textit{sparse}); die euklidische Distanz leidet in solchen Räumen unter dem
sogenannten \textit{Curse of Dimensionality}, da sie alle Dimensionen gleich
gewichtet und von der absoluten Länge der Vektoren abhängt.\footnote{Eine
ausführliche Diskussion des \textit{Curse of Dimensionality} im Kontext
hochdimensionaler Daten findet sich in \cite{Leskovec2020Mining} Kapitel~7.1.}
Zweitens erfassen Lognachrichten desselben Templates häufig dieselben
Schlüsselwörter in ähnlicher Gewichtung, unterscheiden sich jedoch in der
absoluten Häufigkeit dieser Wörter. Die Cosinus-Distanz normiert diesen Effekt
heraus, indem sie ausschließlich den Winkel zwischen zwei Vektoren misst.

\paragraph{Nachbarschaftsradius: \texttt{eps = 0.6}.}
Der Parameter \texttt{DBSCAN\_EPS} definiert den Radius $\varepsilon$, innerhalb
dessen zwei Datenpunkte als Nachbarn gelten. Da die Cosinus-Distanz Werte im
Bereich $[0, 2]$ annimmt – wobei $0$ identische und $2$ vollständig entgegengesetzte
Vektoren bedeutet – entspricht ein Wert von $\varepsilon = 0{,}6$ einer geforderten
Cosinus-Ähnlichkeit von mindestens $0{,}4$ zwischen zwei Lognachrichten. Dieser
Schwellenwert erlaubt es, semantisch ähnliche Lognachrichten eines Templates als
Nachbarn zu erkennen, ohne dabei strukturell verschiedene Nachrichten fälschlich
zusammenzuführen.

\paragraph{Mindestanzahl an Nachbarn: \texttt{min\_samples = 2}.}
Der Parameter \texttt{DBSCAN\_MIN\_SAMPLES} legt fest, wie viele Punkte sich
mindestens im $\varepsilon$-Radius eines Punktes befinden müssen, damit dieser
als Kernpunkt klassifiziert wird. Der Wert $2$ ist die kleinstmögliche sinnvolle
Einstellung und bewirkt, dass bereits zwei sich gegenseitig als Nachbarn erkennende
Lognachrichten einen Cluster bilden. Da der Datensatz pro Log-Template im Mittel
lediglich $10\,000 / n_{\text{Templates}}$ Einträge enthält\footnote{Der verwendete
Datensatz setzt sich aus den Loghub-2k-Quellen \texttt{Linux}, \texttt{Apache} und
\texttt{OpenSSH} zusammen und umfasst nach der Mischung insgesamt $10\,000$ Lognachrichten,
verteilt auf $44$ Ground-Truth-Klassen; siehe Abschnitt~\ref{sec:eval:aufbau}.}, sichert
ein kleiner Wert ab, dass auch selten auftretende Templates nicht fälschlich als Rauschen
klassifiziert werden.

Angewendet wird das Verfahren über \texttt{fit\_predict} auf die zuvor erzeugte
TF-IDF-Matrix \texttt{X\_tfidf}. Datenpunkte in Regionen hoher Dichte werden zu
Clustern zusammengefasst, während Punkte, die keinem Cluster zugeordnet werden
können, als Rauschen klassifiziert werden und das Label $-1$ erhalten. Die
resultierenden Cluster-IDs werden als neue Spalte \texttt{dbscan\_cluster\_id}
im Dataframe gespeichert. Im vorliegenden Testlauf entstanden auf diese Weise
$27$ Cluster, wobei ein nennenswerter Anteil der Lognachrichten als Rauschen
eingestuft wurde – ein charakteristisches Verhalten von DBSCAN bei inhomogen
verteilten Datensätzen.


\section{Drain3 in der Praxis: Iterative Zuordnung durch den Online-Parser}

Die praktische Umsetzung des Clustering-Verfahrens mittels drain3 ist in Listing 4.2 dargestellt. Zunächst erfolgt die Konfiguration des Parsers über das TemplateMinerConfig-Objekt. Ein zentraler Parameter ist hierbei der Ähnlichkeitsschwellenwert (\lstinline{drain_sim_th}), der im vorliegenden Szenario auf $0,2$ festgelegt wird. Dieser Schwellenwert definiert, ab welchem Grad der Ähnlichkeit eine neue Lognachricht einem existierenden Logtemplate im Parse-Baum zugeordnet wird.
Mit der definierten Konfiguration wird anschließend eine Instanz des TemplateMiner initialisiert, welcher die Kernlogik des Drain-Algorithmus kapselt. Entsprechend der Funktionsweise eines Online-Parsers werden die Daten nicht als statischer Block, sondern iterativ verarbeitet. Die Lognachrichten aus der Spalte 'log' des Dataframes \lstinline{df_labeled} werden in einer Schleife einzeln mittels der Methode \lstinline{add_log_message} an den Miner übergeben.
Für jede verarbeitete Nachricht liefert der Algorithmus ein Ergebnisobjekt zurück, aus welchem die identifizierte Cluster-ID extrahiert wird. Diese IDs werden zunächst gesammelt und abschließend als neue Spalte \lstinline{drain_cluster_id} dem Dataframe hinzugefügt. Dadurch ist jeder ursprünglichen Lognachricht das entsprechende Cluster zugeordnet, was die Grundlage für weiterführende Analysen bildet.

\begin{lstlisting}[caption={clustern mit drain3}, label={lst:drain3}]
# Drain3 Parameter
DRAIN_SIM_TH = 0.2

# ===== DRAIN3 CLUSTERING =====
drain_config = TemplateMinerConfig()
drain_config.drain_sim_th = DRAIN_SIM_TH

template_miner = TemplateMiner(config=drain_config)

drain_cluster_ids = []
for log in df_labeled['log']:
    result = template_miner.add_log_message(log)
    drain_cluster_ids.append(result.get("cluster_id"))

df_labeled['drain_cluster_id'] = drain_cluster_ids
\end{lstlisting}

\section{pgvector in der Praxis: Zweistufiges Embedding-basiertes Clustering}
\label{sec:real:pgvector}

Das pgvector-Verfahren unterscheidet sich grundlegend von den bisher vorgestellten
Ansätzen: Anstatt auf statistischen Termgewichtungen (TF-IDF) oder regelbasierten
Baumstrukturen (Drain3) zu operieren, repräsentiert es jede Lognachricht als
semantischen Vektor in einem kontinuierlichen, hochdimensionalen Einbettungsraum.
Ähnliche Lognachrichten liegen in diesem Raum geometrisch nah beieinander, sodass
die Clusterbildung auf Basis von Vektordistanzen erfolgen kann. Die Implementierung
gliedert sich in drei Phasen: die Erzeugung der Embeddings, den Aufbau des
Datenbankschemas sowie das zweistufige Clustering. Die Parameter der Umsetzung
sind:

\begin{itemize}
    \item \texttt{PGVECTOR\_EMBEDDING\_MODEL = "sentence-transformers/all-MiniLM-L6-v2"}
    \item \texttt{PGVECTOR\_INITIAL\_COSINE\_DISTANCE = 0.5}
    \item \texttt{PGVECTOR\_RECLUSTER\_COSINE\_DISTANCE = 0.3}
    \item \texttt{PGVECTOR\_INITIAL\_RATIO = 0.5}
\end{itemize}

\subsection{Embedding-Erzeugung mit Sentence Transformers}

Die Überführung der Lognachrichten in numerische Vektoren übernimmt das
vortrainierte Modell \texttt{sentence-transformers/all-MiniLM-L6-v2} aus der
\texttt{sentence-transformers}-Bibliothek. Das Modell erzeugt für jede
Eingabezeichenkette einen Vektor mit $384$ Dimensionen, der die semantische
Bedeutung des Textes kodiert. Gegenüber TF-IDF-Vektoren, die lediglich das
Vokommen einzelner Terme zählen, erfassen Sentence-Embeddings auch synonyme
Ausdrücke und kontextuelle Bedeutungsunterschiede, da das Modell auf großen
Textkorpora vortrainiert wurde. Das Encoding erfolgt über die Methode
\lstinline{model.encode}, welche alle Lognachrichten in einem Batch verarbeitet
und eine NumPy-Matrix der Form $(n, 384)$ zurückgibt.

\begin{lstlisting}[caption={Embedding-Erzeugung mit all-MiniLM-L6-v2},
                   label={lst:pgvector-embed}]
PGVECTOR_EMBEDDING_MODEL = "sentence-transformers/all-MiniLM-L6-v2"

model = SentenceTransformer(PGVECTOR_EMBEDDING_MODEL)
embeddings = model.encode(logs_clean, show_progress_bar=True)
\end{lstlisting}

\subsection{Datenbankschema: Tabellen und HNSW-Index}

Die erzeugten Embeddings werden in einer PostgreSQL-Datenbank mit der
pgvector-Erweiterung persistiert. Das Schema umfasst zwei Tabellen sowie jeweils
einen HNSW-Index, wie in Listing~\ref{lst:pgvector-schema} dargestellt.

\begin{lstlisting}[caption={Datenbankschema mit HNSW-Indizes},
                   label={lst:pgvector-schema}]
CREATE EXTENSION IF NOT EXISTS vector;

-- Haupt-Tabelle: eine Zeile pro Lognachricht
CREATE TABLE IF NOT EXISTS log (
    id             SERIAL PRIMARY KEY,
    message        TEXT NOT NULL,
    embedding      vector(384),
    processed      BOOLEAN DEFAULT FALSE,
    log_cluster_id INTEGER
);

-- Hilfstabelle: ein Durchschnittsvektor pro Cluster
CREATE TABLE IF NOT EXISTS avg_embedding (
    id             SERIAL PRIMARY KEY,
    avg_embedding  vector(384) NOT NULL,
    log_cluster_id INTEGER NOT NULL UNIQUE
);

-- HNSW-Indizes fuer schnelle Cosinus-Distanz-Suche
CREATE INDEX IF NOT EXISTS idx_log_embedding_hnsw
    ON log USING hnsw (embedding vector_cosine_ops);

CREATE INDEX IF NOT EXISTS idx_avg_embedding_hnsw
    ON avg_embedding USING hnsw (avg_embedding vector_cosine_ops);
\end{lstlisting}

Die Tabelle \texttt{log} speichert neben dem Rohtext und dem Embedding zwei
Steuerfelder: \texttt{processed} kennzeichnet, ob eine Lognachricht bereits einem
Cluster zugewiesen wurde, und \texttt{log\_cluster\_id} hält die resultierende
Cluster-Zugehörigkeit. Die zweite Tabelle \texttt{avg\_embedding} enthält pro
Cluster genau einen Eintrag mit dem arithmetischen Mittelvektor aller zugehörigen
Embeddings. Sie dient als kompakte Repräsentation der bekannten Cluster und
ermöglicht es, neue Lognachrichten effizient gegen bestehende Cluster abzugleichen,
ohne alle Einzelvektoren durchsuchen zu müssen.

Auf beiden Tabellen wird ein HNSW-Index (\textit{Hierarchical Navigable Small World})
mit dem Operator \texttt{vector\_cosine\_ops} angelegt. HNSW baut einen
mehrschichtigen Graphen über die Vektoren auf, in dem jeder Knoten mit einer
begrenzten Anzahl von Nachbarknoten verbunden ist. Eine Suchanfrage traversiert
diesen Graphen von groben zu feinen Schichten und findet so in nahezu konstanter
Zeit die nächsten Nachbarn eines Abfragevektors. Im Vergleich zur alternativen
Indexstruktur IVFFlat bietet HNSW einen besseren Speed-Recall-Tradeoff, benötigt
jedoch mehr Speicher und eine längere Aufbauzeit
\cite{githubGitHubPgvectorpgvector}. Da die Datenmenge in diesem Szenario
überschaubar ist, überwiegen die Vorteile der schnelleren Abfragezeit.

\subsection{Phase 1: Initial Clustering auf der ersten Hälfte der Daten}

Das Clustering folgt einem zweistufigen Ansatz, der die Verarbeitungsreihenfolge
explizit kontrolliert. In Phase 1 werden die ersten 50\,\% der Lognachrichten
(\lstinline{split\_idx = int(len(logs\_clean) * 0.5)}) verarbeitet, um eine initiale
Clusterbasis aufzubauen. Die restlichen Einträge werden zunächst als
\lstinline{processed = TRUE} markiert, sodass der Algorithmus sie in dieser Phase
überspringt. Das Vorgehen ist in Listing~\ref{lst:pgvector-initial} dargestellt.

\begin{lstlisting}[caption={Phase 1: Initial Clustering},
                   label={lst:pgvector-initial}]
PGVECTOR_INITIAL_COSINE_DISTANCE = 0.5
PGVECTOR_INITIAL_RATIO           = 0.5

split_idx = int(len(logs_clean) * PGVECTOR_INITIAL_RATIO)

# Zweite Haelfte temporaer sperren
conn.execute(text(
    "UPDATE log SET processed = TRUE WHERE id > :s"
), {"s": split_idx})

# Initial Clustering auf erster Haelfte
for (log_id,) in candidates:
    # Bereits zugewiesene Logs ueberspringen
    if already_assigned(log_id):
        continue

    # Aehnliche Logs per Cosinus-Distanz finden
    similar = conn.execute(text("""
        WITH target AS (SELECT embedding FROM log WHERE id = :id)
        SELECT l.id FROM log l, target t
        WHERE l.processed = FALSE
          AND (l.embedding <=> t.embedding) < :threshold
        ORDER BY l.embedding <=> t.embedding
    """), {"id": log_id,
           "threshold": PGVECTOR_INITIAL_COSINE_DISTANCE})

    if similar:
        new_cluster_id = max_cluster_id() + 1
        assign_cluster(similar_ids, new_cluster_id)

# Durchschnittsvektoren berechnen
INSERT INTO avg_embedding (log_cluster_id, avg_embedding)
SELECT log_cluster_id, AVG(embedding) FROM log
WHERE log_cluster_id IS NOT NULL
GROUP BY log_cluster_id
ON CONFLICT (log_cluster_id) DO UPDATE
    SET avg_embedding = EXCLUDED.avg_embedding;
\end{lstlisting}

Der Algorithmus iteriert sequenziell über alle noch nicht zugewiesenen
Kandidaten. Für jeden Kandidaten wird über den HNSW-Index eine Nachbarschaftssuche
mit dem Schwellenwert \texttt{PGVECTOR\_INITIAL\_COSINE\_DISTANCE = 0.5} ausgeführt:
Alle Logs, deren Cosinus-Distanz zum aktuellen Kandidaten diesen Wert unterschreitet,
gelten als ähnlich genug für eine gemeinsame Cluster-Zugehörigkeit. Der gesamte
Ähnlichkeitsbatch erhält eine neue Cluster-ID. Nach Abschluss aller Zuweisungen
werden die Durchschnittsvektoren je Cluster in \texttt{avg\_embedding} berechnet
und gespeichert.

\subsection{Phase 2: Re-Clustering auf der zweiten Hälfte der Daten}

In Phase 2 werden die zurückgehaltenen 50\,\% der Lognachrichten verarbeitet.
Diese werden als \lstinline{processed = FALSE} markiert und iterativ gegen die
bereits bekannten Cluster abgeglichen. Das Re-Clustering folgt einer
Drei-Strategien-Logik, die in Listing~\ref{lst:pgvector-recluster} dargestellt ist.

\begin{lstlisting}[caption={Phase 2: Re-Clustering mit drei Strategien},
                   label={lst:pgvector-recluster}]
PGVECTOR_RECLUSTER_COSINE_DISTANCE = 0.3

for (log_id,) in unprocessed:

    # Strategie 1: Aehnlichen Durchschnittsvektor suchen
    similar_cluster = conn.execute(text("""
        WITH target AS (SELECT embedding FROM log WHERE id = :id)
        SELECT ae.log_cluster_id FROM avg_embedding ae, target t
        WHERE (ae.avg_embedding <=> t.embedding) <= :threshold
        ORDER BY ae.avg_embedding <=> t.embedding LIMIT 1
    """), {"id": log_id,
           "threshold": PGVECTOR_RECLUSTER_COSINE_DISTANCE})

    if similar_cluster:
        assign_to_existing_cluster(log_id, similar_cluster)

    else:
        # Strategie 2: Aehnliches Einzel-Log suchen
        similar_log = conn.execute(text("""
            WITH target AS (SELECT embedding FROM log WHERE id = :id)
            SELECT l.log_cluster_id FROM log l, target t
            WHERE l.processed = TRUE
              AND (l.embedding <=> t.embedding) < :threshold
            ORDER BY l.embedding <=> t.embedding LIMIT 1
        """), {"id": log_id,
               "threshold": PGVECTOR_INITIAL_COSINE_DISTANCE})

        if similar_log:
            assign_to_existing_cluster(log_id, similar_log)
        else:
            # Strategie 3: Neuen Cluster anlegen
            assign_new_cluster(log_id)

    # Durchschnittsvektor aktualisieren
    update_avg_embedding(log_id)
\end{lstlisting}

Die drei Strategien sind nach aufsteigendem Rechenaufwand geordnet. Strategie~1
sucht zunächst in der \texttt{avg\_embedding}-Tabelle nach einem ähnlichen
Cluster-Schwerpunkt; dieser Vergleich ist besonders effizient, da die Tabelle
nur so viele Einträge enthält, wie Cluster existieren – deutlich weniger als die
Gesamtzahl der Lognachrichten. Der Schwellenwert von
\texttt{PGVECTOR\_RECLUSTER\_COSINE\_DISTANCE = 0.3} ist dabei bewusst strenger
als in Phase 1 ($0{,}5$) gewählt: Ein neues Log soll einem bestehenden Cluster
nur dann zugeordnet werden, wenn es sich in unmittelbarer Nähe des
Cluster-Schwerpunktes befindet, um Cluster-Drift durch sukzessive Fehlzuweisungen
zu vermeiden. Findet Strategie~1 keinen Treffer, weitet Strategie~2 die Suche
auf alle verarbeiteten Einzellogs aus. Erst wenn auch diese Suche erfolglos
bleibt, legt Strategie~3 einen neuen Cluster an. Nach jeder Zuweisung wird der
betroffene Durchschnittsvektor in \texttt{avg\_embedding} aktualisiert, sodass
nachfolgende Zuweisungen auf dem aktuellen Stand der Clusterbasis operieren.

Im vorliegenden Testlauf erzeugte Phase~1 insgesamt 28 Cluster; Phase~2 erstellte
keine neuen Cluster, da alle verbleibenden Lognachrichten einem der bestehenden
Cluster zugeordnet werden konnten. Das Endergebnis von 28 Clustern entspricht
damit in guter Näherung der tatsächlichen Anzahl der Ground-Truth-Klassen im
verwendeten Loghub-Datensatz.